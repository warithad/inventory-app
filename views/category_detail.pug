extends layout

block content 
    h1= title
    p #[strong Category Name:] #{category.name}
    p #[strong Description:] #{category.description}
    ul 
        each item in category_items
            li 
                a(href=item.url) #{item.name}
                |   $#{item.price}
        else 
            li There are no items    
